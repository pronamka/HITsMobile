package com.example.hit.language.parser

enum class TokenType {
    INT,
    DOUBLE,
    STRING,
    BOOL,
    PLUS,
    MINUS,
    ASTERISK,
    SLASH,
    WORD,
    EQUALS,
    LEFT_BRACE,
    RIGHT_BRACE,
    EOF;
}